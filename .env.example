# Foundry Local Streaming Validation – .env example
# Copy to .env and adjust values

# ── Foundry Local connection ──────────────────────────────
# FOUNDRY_BASE_URL is optional. If omitted, the tool runs
# `foundry service status` to auto-detect the dynamic port.
# FOUNDRY_BASE_URL=http://127.0.0.1:5272/v1

# FOUNDRY_MODEL is optional. If omitted, the tool fetches the
# Foundry Local model catalog (/v1/models) and presents an
# interactive picker so you can choose at runtime.
# FOUNDRY_MODEL=phi-4-mini

FOUNDRY_API_KEY=unused

# ── Copilot SDK BYOK configuration ───────────────────────
COPILOT_BYOK_PROVIDER_TYPE=openai
COPILOT_WIRE_API=completions

# ── Timeout tuning (milliseconds) ────────────────────────
REQUEST_TIMEOUT_MS=30000
FIRST_BYTE_TIMEOUT_MS=10000
FIRST_EVENT_TIMEOUT_MS=15000
